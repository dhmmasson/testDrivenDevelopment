# Debug

Pandora should not output anything but the requested output or full report. In particular, all your debug messages should be hidden unless you pass the ```-d``` option to the [CLI](../Resources/CLI.md). You can use the ```-d``` option to print additional debug information on unhandled exceptions. This is useful to understand what went wrong when your program crashes without giving any output.

## Debug in VsCode
